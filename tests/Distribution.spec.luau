local library, window = example()
assert(#requests == 3, "Example must load three independent files")
assert(library.Toggles.Enabled.Value)
assert(library.Options.Volume.Value == 50)
assert(window:GetTab("Main"):GetSubTab("Controls"))
assert(window:GetTab("Settings"):GetSubTab("Theme"))
assert(window:GetTab("Settings"):GetSubTab("Config"))
assert(library.Options.MenuKeybind.Value == "RightShift")
assert(#library:Validate() == 0)
library:Unload()
assert(library.Unloaded and not library.Mounted)
local second, secondWindow = example()
assert(second ~= library and secondWindow ~= window)
assert(#second:Validate() == 0)
second:Unload()
print("PASS distribution example, independent managers, cleanup, and rerun")
